# Reviewer-supplement figure specification

No paper-facing artwork is generated overnight. Any later rendering must follow
`experiments/icra27_crosssuite_query_allocation/ONE_CLOCK_STYLE_GUIDE.md`.

1. R1A: two source-age series over the complete frozen grid
   `0,2,4,8,12,16,20,32`, with paired and task-cluster intervals. The Fresh
   anchor is common. Do not select or highlight a best age.
2. R1B: paired translation-stale and rotation-stale estimates at d=20, each
   against Fresh and against one another. State the remaining semantic limits.
3. R1C: a four-cell dense-query H16 factorial or simple-effect display, labeled
   `POST_HOC_QUERY_MATCHED_EXTENSION`. Do not visually merge it with the
   original confirmation.
4. R1D: if completed, show the Spatial factorial separately and label it
   `POST_HOC_SPATIAL_FACTORIAL_COMPLETION`.
5. R2A: if completed, show the SmolVLA four-cell scope factorial separately.
   Do not imply independent confirmation.

Diagnostic fixed-source A0G20/A20G0 points must not be connected to the
coherent-horizon Track-A dose-response curve.
