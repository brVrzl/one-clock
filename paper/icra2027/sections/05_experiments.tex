\section{Experiments}
\label{sec:experiments}

\subsection{Static horizon analysis}

\paragraph{Common setup.}

All experiments use the same frozen ACT checkpoint for LIBERO Object, with a
prediction horizon of 100 and seven-dimensional relative actions.  Dimensions
0--5 form the arm/end-effector group and dimension 6 the gripper group.  Control
runs at 20 Hz.  Temporal ensembling is disabled, no policy weights are updated,
and compared configurations use the same checkpoint and paired official initial
states.  Episode success is the benchmark success condition.  Policy-query rate
is the number of full policy evaluations divided by environment steps.

Each episode is seeded by 1000 plus its official initial-state index and stops on
benchmark success or the environment's 280-step limit.  The policy output is
checked at runtime to have shape $100\!\times\!7$.  We log every environment
step, policy query, group cursor, source-generation identifier, and source age;
the paper's reported numbers come from committed aggregates over these logs.
As an implementation check, all five task-0 diagonal group configurations
reproduce their corresponding global success, episode length, and query counts.

We report paired success counts and, for selected comparisons, a two-sided exact
McNemar diagnostic.  These small-sample diagnostics are descriptive; task-level
tests are not corrected for the many explored configurations.

\paragraph{Task-0 landscape.}

For `pick up the alphabet soup and place it in the basket,' we evaluate official
initial states 0--49.  Global controls use
$h\in\{1,2,4,8,16\}$.  Group-specific execution evaluates the complete
$5\!\times\!5$ Cartesian product over the same horizon set, producing exact
diagonal controls and ten symmetric assignment pairs $(h_a,h_g)$ versus
$(h_g,h_a)$.  This protocol comprises 1,500 paired episodes.

\paragraph{Cross-task diagnostic.}

We next evaluate LIBERO Object tasks 1--9 on official states 0--19 per task,
with seeds 1000--1019.  The common coarse set contains diagonal/global-equivalent
horizons 2, 4, 8, and 16 and eight off-diagonal pairs:
$(2,8)$, $(2,16)$, $(8,2)$, $(8,16)$, $(16,2)$, $(16,8)$,
$(4,16)$, and $(16,4)$.  No task-specific policy training or tuning is performed.
The coarser grid makes per-task best points retrospective over the evaluated set,
not estimates of a continuously optimized method.

Table~\ref{tab:protocols} summarizes the two protocols.  The 2,180 cross-task
episodes include one additional 20-episode raw diagonal duplicate on task 1,
run for implementation validation; scientific comparisons use 12 unique static
configurations per task.

\begin{table}[t]
\caption{Evaluation protocols and configuration accounting.  Task 0 uses five
global and 25 group-specific configurations, with five diagonal group-specific
entries duplicating the corresponding global semantics as controls.  Cross-task
comparisons use 12 unique static configurations per task plus one additional
20-episode raw diagonal duplicate on task 1 for implementation validation
(2,180 rollouts total).}
\label{tab:protocols}
\centering
\scriptsize
\setlength{\tabcolsep}{3.0pt}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lccc}
\toprule
Protocol & States/task & Configuration accounting & Episodes \\
\midrule
Task 0 landscape & 50 & 5 global + 25 group-specific; 5 diagonal controls & 1,500 \\
Cross-task (1--9) & 20/task & 12 unique/task + 1 task-1 validation duplicate & 2,180 \\
\bottomrule
\end{tabular}
}%
\end{table}

\subsection{Group temporal persistence analysis}

The static rollout protocols are complemented by two read-only audits of the
same frozen ACT policy on LIBERO Object demonstrations.  Gate-1 evaluates
complete 100-step predictions at 1,098 observation points from all 454
demonstration episodes.  Starts are sampled every 50 frames, with the final
valid full-chunk start included; predicted actions are compared with the
demonstrated future actions separately for arm translation, arm rotation, and
gripper metrics.  This is teacher-forced prediction analysis, not an executor
rollout or a success evaluation.

Gate-2A tests phase dependence with a separate predeclared protocol: starts are
sampled every 25 frames together with the one-third and two-thirds episode
locations, producing 3,740 points and 454 episodes in each normalized phase
(early, middle, late).  Because late targets are right-censored, all primary
phase comparisons use the common future range $k=0,\ldots,37$ and average within
episode before aggregating.  Episode-bootstrap intervals are descriptive.  The
phase audit likewise does not train a predictor, change the executor, or measure
closed-loop task success.

\subsection{Reliability estimator evaluation (planned)}

No group-wise reliability estimator has been trained, calibrated, or evaluated
in this paper.  The planned evaluation will first use held-out demonstration
episodes to measure validity-proxy discrimination and survival calibration at
each offset, with censoring and task identity reported explicitly.  It will
then compare the resulting observation-to-horizon rule against fixed global and
fixed group-wise schedules under the same frozen policy, seeds, and policy-query
accounting.  Closed-loop success, policy-inference demand, source-age disparity,
and cross-group consistency will be reported together.  This subsection is a
protocol placeholder for future validation, not a completed experiment or an
adaptive-execution result.
