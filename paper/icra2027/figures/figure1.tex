% Draft content placeholder only; final paper-facing artwork is intentionally absent.
\begin{figure*}[t]
\centering
\fbox{\parbox[c][28mm][c]{0.96\textwidth}{\small
\textbf{Draft Fig. 1 specification.} Left: a control-step timeline showing
$A_t[0]$ and $A_{t-20}[20]$ converging on physical target $t$, with
$q+k=t$, $d=20=1.00$~s at 20~Hz, and the all-Fresh prefix for $t<20$.
Center: A0G20 uses fresh arm/stale gripper; A20G0 uses stale arm/fresh
gripper. Right: the preregistered 140-block factorial, visually emphasizing
only A0G20$-$A20G0=$+32.14$ points, paired CI $[+23.6,+40.7]$, and
task-cluster CI $[+21.4,+44.3]$.}}
\caption{\textbf{Same-target diagnostic and primary preregistered result.}
At step $t$, A0G20 combines the arm from query $t$, offset 0, with the gripper
from query $t-20$, offset 20; A20G0 swaps those assignments. Both commands
target step $t$. Moving the one-second-old prediction from the gripper to the
arm changed success from 83/140 to 38/140, a 32.14-point difference. These
probes query the policy once per executed step (policy-query rate 1.0) and are
diagnostic interventions rather than deployment executors.}
\label{fig:primary}
\end{figure*}
